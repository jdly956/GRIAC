# Plan de test S1.5 — Client Albert & sonde des limites

**Environnement cible** : pod Onyxia VSCode **sans GPU**, mode B. **Réseau requis vers Albert** (`https://albert.api.etalab.gouv.fr`). La clé est déjà injectée dans l'environnement du pod (constat S1.4) ; `ALBERT_BASE_URL` vient du `.env` créé en S1.4.

**Enjeu** : c'est le **test no-go n°1** de la note de cadrage (§6) — relever la fenêtre effective et les quotas réels avant tout tuning. Comparer notamment la fenêtre servie au budget de contexte ≤ 20 000 tokens (CLAUDE.md).

**Clôture** : consigner les résultats dans `SESSIONS.md` et **committer le rapport `docs/albert-limits.md`** (il ne contient ni clé ni identifiant — vérifié à l'étape 5).

## 0. Préparation

```bash
cd ~/work/GRIAC/
git fetch origin && git checkout <branche-de-la-PR-S1.5> && git pull
make install
source .venv/bin/activate
printenv ALBERT_API_KEY | wc -c     # attendu : > 1 (clé dans l'env du pod)
grep -c '^ALBERT_BASE_URL=.\+' .env # attendu : 1 (URL dans le .env de S1.4)
```

## 1. TNR — baseline complète (TU inclus)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint
make test
```

Attendu : lint vert ; **23 tests verts** (dont les 10 TU S1.5 : `test_albert.py`, `test_probe.py`, timeout/retries dans `test_config.py`).

## 2. Erreurs gérées — clé invalide (à faire AVANT le relevé nominal)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
ALBERT_API_KEY=cle-invalide-test make probe ; echo "code retour = $?"
```

Attendu : les 4 relevés en `échec — …` avec un message court (401/AuthenticationError), **aucun traceback**, code retour non nul. Le rapport d'échec écrit ici sera écrasé par l'étape 3.

## 3. Relevé nominal

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make probe ; echo "code retour = $?"
```

Attendu : `Rapport écrit : docs/albert-limits.md`, les 4 relevés `ok` (`modeles`, `quotas`, `chat`, `embeddings`), code retour **0**.

## 4. Inspection du rapport (le livrable du test no-go n°1)

```bash
cd ~/work/GRIAC/
cat docs/albert-limits.md
```

Attendu, dans l'ordre :
- tableau **Modèles servis** — y repérer les alias `openweight-large`, `openweight-embeddings`, `openweight-rerank` (ou leurs cibles gpt-oss-120b / bge-m3 / bge-reranker-v2-m3) et la **fenêtre de contexte** (`max_context_length`) ;
- objet **`limits`** (quotas TPM/TPD/RPM du compte) ;
- **chat** : `reponse` non vide, `modele_resolu`, latence ;
- **embeddings** : `dimension` = **1024** (bge-m3).

## 5. Aucune fuite de clé dans le rapport

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
CLE="${ALBERT_API_KEY:-$(grep '^ALBERT_API_KEY=' .env | cut -d= -f2-)}"
test -n "$CLE" || echo "⚠️ CLE vide : STOP — un grep sur chaîne vide matche TOUTES les lignes"
grep -c -- "$CLE" docs/albert-limits.md || echo "OK : 0 occurrence de la clé dans le rapport"
```

Attendu : pas de ⚠️ ; `0` occurrence. Vérifier aussi visuellement qu'aucun email/identifiant du compte n'apparaît (la sonde ne garde que l'objet `limits`).

## 6. Clôture — committer le relevé

```bash
cd ~/work/GRIAC/
git add docs/albert-limits.md
git commit -m "docs(albert): relevé des limites — make probe (S1.5)"
git push
```

Puis consigner dans `SESSIONS.md` (ou coller la sortie des étapes 1–5 en session Claude, qui consignera) : fenêtre effective relevée, quotas, verdict no-go n°1 (la fenêtre servie couvre-t-elle le budget de 20 000 tokens ?).
