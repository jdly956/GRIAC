# Plan de test S2.4 — Rerank + assemblage du contexte (E2)

**Environnement cible** : chaîne complète jouée (plans S1.7 → S2.3) + clé Albert (env du pod).

## 0. Schéma réel de `/v1/rerank` — CONFIRMÉ (pod Onyxia, 03/07/2026)

L'hypothèse initiale (`{model, prompt, input}` → `data[{index, score}]`) était **fausse** (422). Le schéma réel, désormais implémenté : `{model, query, documents}` → `results[{index, relevance_score}]`. Contre-épreuve rejouable :

```bash
cd ~/work/GRIAC/
BASE=$(grep '^ALBERT_BASE_URL=' .env | cut -d= -f2-)
curl -sS -o /tmp/rerank.json -w "HTTP %{http_code}\n" "$BASE/rerank" \
  -H "Authorization: Bearer $ALBERT_API_KEY" -H "Content-Type: application/json" \
  -d '{"model":"openweight-rerank","query":"délai de traitement","documents":["Le délai d'\''instruction est de 15 jours.","La couleur du logo est bleue."]}'
head -c 400 /tmp/rerank.json ; echo
```

Attendu : `HTTP 200` et `results` avec `index`/`relevance_score`, le 1er document nettement mieux scoré que le 2nd (relevé du 03/07 : 0,73 vs 1,6e-05).

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate && make lint && make test
```

Attendu : lint vert ; **112 tests verts** (dont 9 TU S2.4, `api/tests/test_contexte.py`).

## 2. Contexte assemblé et cité (api lancée)

```bash
curl -sS -X POST http://localhost:8000/contexte -H "Content-Type: application/json" \
  -d '{"question": "temps de connexion au module d'\''authentification"}' | python3 -m json.tool
```

Attendu : `contexte` composé de blocs `[Source : <nom> — <section>]`, `sources` listées (document + section), `nb_tokens` ≤ 6000, **`rerank_applique: true`** (si étape 0 concluante), `avertissement: null`.

## 3. Repli signalé si rerank indisponible

```bash
ALBERT_MODEL_RERANK=modele-inexistant uv run --package sia-api uvicorn sia_api.main:app --port 8001 &
sleep 2 && curl -sS -X POST http://localhost:8001/contexte -H "Content-Type: application/json" \
  -d '{"question": "authentification"}' | python3 -m json.tool
kill %2
```

Attendu : réponse 200, `rerank_applique: false`, `avertissement: "Rerank indisponible : ordre de la fusion RRF conservé."` — jamais d'échec silencieux.

## 4. Aucune source

```bash
curl -sS -X POST http://localhost:8000/contexte -H "Content-Type: application/json" \
  -d '{"question": "quotas de pêche en mer Baltique"}' | python3 -m json.tool
```

Attendu : `contexte: ""`, `avertissement: "Aucune source récupérable…"`. Le déclenchement repose sur le seuil de distance cosinus du volet vectoriel (`RECHERCHE_SEUIL_DISTANCE`, défaut 0,55 — calibré sur les fixtures : pertinent ≤ 0,431, hors corpus ≥ 0,698) : sans seuil, les K plus proches voisins « répondent » toujours et l'avertissement ne peut jamais partir (constaté pod 03/07/2026).

## 5. Clôture

Consigner dans `SESSIONS.md` (schéma rerank confirmé ou écart relevé), cocher les CA de S2.4.
