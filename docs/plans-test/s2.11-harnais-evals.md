# Plan de test S2.11 — E6 : harnais d'évals `make eval` (grille 3 axes)

**Environnement cible** : pod Onyxia avec la clé Albert (`.env` ou env du pod). **Pas de base requise** : le harnais lit `evals/` et appelle Albert directement. Attention au quota journalier (tpd 2,46 M — S1.5) : le banc complet fait 3 cas × 2 modèles = 6 générations (~30 k tokens).

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **197 tests verts** (dont 17 TU `test_evaluation.py` — scoring pur + client simulé).

## 2. Fumée sans réseau (chargement des cas)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
uv run --package sia-api python -c "
from sia_api.evaluation import charger_cas
cas, origine = charger_cas()
print(origine, len(cas), [c.titre for c in cas])
print('--- brief du cas 1 ---'); print(cas[0].brief)
"
```

Attendu : `silver 3` (tant que `evals/gold/` est vide) + les 3 titres ; le brief contient récit/règles métier/attendus mais **pas** les CA.

## 3. Banc réduit (1 cas, 1 modèle — ~2 min)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
uv run --package sia-api python -m sia_api.evaluation --modeles openweight-large --max-cas 1
```

Attendu : bandeau « Références SILVER … scores indicatifs », tableau avec les 4 scores (gabarit/exactitude/complétude/moyenne), durée et tokens de sortie renseignés, aucune erreur. La clé n'apparaît nulle part dans la sortie.

## 4. Banc complet comparatif + rapport

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make eval SORTIE=docs/eval-$(date +%Y%m%d).md
```

Attendu : 6 lignes (3 cas × `openweight-large`,`openweight-medium`), section « Moyennes par modèle » avec un classement exploitable, rapport écrit. Consigner le classement et les latences dans `SESSIONS.md` (préparation du choix de modèle — verdict E6).

## 5. Robustesse

1. Modèle inexistant : `uv run --package sia-api python -m sia_api.evaluation --modeles alias-fantome --max-cas 1` → la colonne Erreur porte l'échec, le banc ne s'arrête pas, exit 0.
2. Sans références : renommer temporairement `evals/silver/stories-silver-candidates.md`, relancer → message explicite + exit 1 ; **remettre le fichier**.

## 6. Recalibrage gold (dès fourniture)

Déposer les stories validées dans `evals/gold/*.md` (format `---` + `**US — …**`) → `make eval` bascule automatiquement (le bandeau silver disparaît). Vérifier `origine=gold` via l'étape 2.

## 7. Clôture

Consigner résultats et classement dans `SESSIONS.md`, cocher les CA de S2.11.
