# Plan de test S2.15 — anti-invention v2 : consigne de marquage durcie + dédup sémantique du registre

**Environnement cible** : pod Onyxia, branche PR #30 à jour (`git pull` — vérifier `make test` = 229), api relancée (`make pod-up`). Aucune migration.

**Constats d'origine (sessions réelles 9/10/11)** : registre à 18 « en attente » en session 11 — récapitulatifs re-listant les hypothèses sous d'autres mots ; variantes de marqueur du modèle (`[HYPOTHÈSE 1 A]`, `[HYPOTHÈSE À VALIDER 1]`) perdues ; valeurs chiffrées inventées affirmées sans marqueur (rétentions, horaires de job, volumétries).

## 1. TNR + fumée sans réseau

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 229 verts
uv run --package sia-api python -c "
from sia_api.workflow import est_doublon_hypothese
existants = [\"- Taille maximale d'une pièce jointe : 10 Mo [HYPOTHÈSE À VALIDER]\"]
recap = '- **#3** : Taille maximale de la pièce jointe = 10 Mo, comme indiqué dans les critères [HYPOTHÈSE À VALIDER].'
autre = '- Notification par courriel à chaque changement de statut [HYPOTHÈSE À VALIDER]'
print('doublon:', est_doublon_hypothese(recap, existants), '| distincte:', est_doublon_hypothese(autre, existants))
"
```

Attendu : `doublon: True | distincte: False` — la reformulation est absorbée, l'hypothèse nouvelle jamais avalée.

## 2. En session réelle — le scénario de la session 11 rejoué

Reprendre la Feature du scénario `s2.13-scenario-rejeu-pod.md` §2 (2 hypothèses amorcées), dérouler interview → candidates → rédaction + DoR de 2-3 stories, puis demander explicitement :

```
Fais un récapitulatif complet de la Feature et de toutes les hypothèses.
```

**Attendus** :
1. **Registre stable** : le récapitulatif ne fait PAS grossir le panneau « Hypothèses à valider » (session 11 : chaque récap ajoutait ses reformulations). Comparer le compteur avant/après.
2. **Marqueur exact** : pas de `[HYPOTHÈSE 1 A]` ni `[HYPOTHÈSE À VALIDER 1]` dans les réponses ; les alternatives A/B/C proposées au choix ne portent pas le marqueur.
3. **Valeurs chiffrées** : demander « propose une durée de rétention des pièces » sans corpus ni précision → la valeur proposée porte `[HYPOTHÈSE À VALIDER]` sur sa ligne.

## 3. Garde-fou A8 (contre-épreuve de non-perte)

Dans la même session, énoncer une hypothèse **réellement nouvelle mais proche lexicalement** :

```
Il faudra aussi une taille maximale de 50 Mo pour les pièces déposées par les
agents (différente de celle des usagers).
```

Attendu : si le modèle la marque, elle **entre au registre** (formulation distincte — la dédup ne l'avale pas). Si elle n'apparaît pas, l'ajouter à la main (`POST /hypotheses`) fonctionne toujours.

## 4. Limites assumées (v2, POC)

- La dédup est lexicale (recouvrement de termes ≥ 0,8) : une reformulation TRÈS éloignée peut encore passer ; le PO garde la main (Rejeter).
- Le respect des consignes de marquage dépend du modèle — en cas de dérive persistante d'`openweight-medium`, consigner les verbatims dans `SESSIONS.md` pour recalibrage au gold (E6).

## 5. Clôture

Consigner compteurs avant/après et verbatims dans `SESSIONS.md`, cocher les CA de S2.15 (sprint-2).
