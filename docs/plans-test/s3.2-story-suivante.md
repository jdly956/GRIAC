# Plan de test S3.2 — bouton « Story suivante » aux étapes de production

**Environnement cible** : pod Onyxia, branche PR #30 à jour (`git pull`, `make test` = 232), api + web relancées (`make pod-up`). Aucune migration.

**Arbitrage d'origine (06/07/2026)** : le cycle réel est « une story = rédaction + contrôle DoR » (sessions 9/11) — le bouton itère de story en story **sans toucher la machine à états** ; le « Oui — étape suivante » reste le geste explicite quand toutes les stories sont couvertes. Badge A5 fidèle.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 232 verts
```

## 2. En session réelle (navigateur)

1. Dérouler une session (Feature du scénario `s2.13-scenario-rejeu-pod.md` §2) jusqu'à l'**étape 3 — Rédaction** ; demander la rédaction de la première story.
2. **Attendus à l'écran** : panneau « Story suivante » visible (absent aux étapes 0–2 — recharger une session à l'interview pour le vérifier) ; le panneau « Valider l'étape » porte le rappel « quand toutes les stories candidates sont couvertes ».
3. **Attendu moteur (consigne « une seule story »)** : la réponse rédige UNE story + son DoR, puis annonce le nombre de candidates restantes — sans enchaîner spontanément (et sans troncature MAX_TOKENS, constat session 8).
4. Cliquer **« Story suivante »** → la story suivante arrive (rédaction + DoR) et le **badge d'étape ne bouge pas** (reste « 3 — Rédaction ») — c'est LE test A5. Recouper en base si besoin : `SELECT etape FROM workflow_sessions WHERE id = <id>;` → inchangée.
5. Répéter jusqu'à « il ne reste aucune story à rédiger » → cliquer **« Oui — étape suivante »** : l'étape avance (règle 5 intacte), la validation est journalisée (télémétrie E4.4 inchangée).

## 3. Non-régression adjacente

- Le « Non — itérer » fonctionne comme avant (itération avec commentaire).
- Les contrôles gabarit/DoR continuent de tomber sur chaque story produite via « Story suivante » (correctif session 9 : DoR contrôlé à toute étape de production).
- Levées proposées (S2.13) et dédup du registre (S2.15) inchangées.

## 4. Clôture

Consigner dans `SESSIONS.md` (badge stable + verbatim « stories restantes »), cocher les CA de S3.2 (sprint-3).
