# Plan de test S3.8 — dynamisme htmx (envoi sans rechargement, indicateur, anti double-clic)

**Environnement cible** : pod Onyxia, `main` à jour après merge de la PR S3.8, web relancée (`make pod-up`). Aucune migration. htmx est **vendoré** (`web/sia_web/static/htmx.min.js`, servi par l'app — aucun CDN).

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 239 verts
curl -s http://localhost:8081/static/htmx.min.js | head -c 40   # attendu : var htmx=function(){...
```

## 2. En session réelle (navigateur) — le test central

1. Ouvrir une session, envoyer un message. **Attendus** :
   - **« ⏳ Génération en cours… » s'affiche** à côté du bouton pendant l'appel (~7-30 s) ;
   - les boutons du formulaire sont **désactivés** pendant l'appel — recliquer ne renvoie rien (anti double-envoi, le défaut qui guettait sur les longues générations) ;
   - la page se met à jour **sans rechargement blanc** ; la « Dernière réponse » apparaît en haut (S3.7).
2. Même vérification sur « Story suivante » et « Valider l'étape » (Oui et Non — la valeur du bouton cliqué est bien transmise).
3. **Via le proxy code-server** (`/proxy/8081/`) : vérifier que `htmx.min.js` charge (onglet Réseau : 200 sur `/proxy/8081/static/htmx.min.js`) et que les envois fonctionnent à l'identique.

## 3. Repli sans JavaScript (progressive enhancement)

Désactiver JavaScript dans le navigateur (ou bloquer le fichier statique) → les trois formulaires fonctionnent comme avant en POST classiques avec rechargement. Aucune fonctionnalité perdue.

## 4. Non-régression adjacente

Boutons Confirmer/Rejeter des hypothèses et notation : inchangés (POST classiques — appels rapides, pas boostés). Exports inchangés.

## 5. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S3.8 (sprint-3).
