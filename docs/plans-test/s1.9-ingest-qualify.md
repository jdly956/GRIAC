# Plan de test S1.9 — Qualification v0 (métadonnées & versions)

**Environnement cible** : PostgreSQL migrée (0001→0004), inventaire des fixtures déjà chargé (plan S1.7 étape 3). Enchaînable directement après les plans S1.7 et S1.8 sur la même base.

**Clôture** : consigner dans `SESSIONS.md`, cocher les CA de S1.9.

## 0. Préparation

```bash
cd ~/work/GRIAC/
git fetch origin && git checkout <branche-de-la-PR-S1.9> && git pull
make install
source .venv/bin/activate
export DATABASE_URL="postgresql+psycopg://<...>"   # comme aux plans S1.7/S1.8
```

## 1. TNR — baseline complète

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **49 tests verts** (dont 9 TU S1.9, `ingestion/tests/test_qualify.py` — jeu piégé du CA4 inclus).

## 2. Migration 0004

```bash
cd ~/work/GRIAC/api/ && uv run alembic upgrade head
```

Attendu : `Running upgrade 0003 -> 0004` (colonnes projet_suggere, date_nom, version_no, marque_finale, statut_brouillon, groupe_version, est_reference, doublon_de).

## 3. Inventaire préalable (si base vide)

```bash
cd ~/work/GRIAC/ && make ingest-scan CORPUS=evals/fixtures
```

## 4. Qualification du lot

```bash
make ingest-qualify ; echo "code retour = $?"
```

Attendu : `6 documents qualifiés — 5 références, 1 doublons, 0 brouillons. NB : projet_suggere est une suggestion (A6)…`, code retour 0. Lecture : le doublon (copie) n'est jamais référence ; chaque groupe de versions a exactement une référence (la spec v2 finale) ; les documents seuls dans leur groupe sont leur propre référence.

## 5. Le jeu piégé, vu de la base

```sql
select nom, projet_suggere, version_no, marque_finale, statut_brouillon,
       groupe_version, est_reference, doublon_de, date_nom
from documents order by chemin;
```

Attendu :
- les 3 specs partagent `groupe_version = 'spec-authentification'` et `projet_suggere = 'projet-alpha'` ;
- `spec-authentification_v2_final_VF3.docx` : `version_no = 2`, `marque_finale = t`, **`est_reference = t`** ;
- `…v1.docx` : `version_no = 1`, `est_reference = f` ;
- `… - Copie.docx` : **`doublon_de = projet-alpha/spec-authentification_v2_final_VF3.docx`**, `est_reference = f` ;
- `notes-reunion-2026-06-15.txt` : **`date_nom = 2026-06-15`**, `projet_suggere = 'projet-beta'`.

## 6. Idempotence

```bash
make ingest-qualify
```

Attendu : mêmes compteurs, mêmes lignes (relance sans effet de bord — la qualification recalcule tout).

## 7. Clôture

Consigner les sorties (étapes 1, 4–6) dans `SESSIONS.md` ; cocher les CA de S1.9. Rappel A6 : `projet_suggere` reste une suggestion tant que le PO n'a pas confirmé via S1.11.
