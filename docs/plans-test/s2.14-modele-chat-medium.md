# Plan de test S2.14 — bascule du modèle de chat par défaut vers `openweight-medium` (à l'essai)

**Environnement cible** : pod Onyxia (clé Albert + base peuplée), api + web lancés (`make pod-up`).

**Contexte** : verdict E6 v0 (06/07/2026, `docs/eval-onyxia.md`) — `openweight-medium` 0,806 > `openweight-large` 0,498 (dérive de format de large, corroborée en session réelle 8). Bascule **à l'essai**, réversible par `ALBERT_MODEL_CHAT=openweight-large` dans le `.env` (aucun changement de code). Recalibrage définitif au gold.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **220 tests verts** (les TU du défaut vérifient désormais `openweight-medium` ; la TU de surcharge prouve le chemin de retour vers large).

## 2. Vérifier le défaut effectif et l'alias servi

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make probe
```

Attendu : la sonde appelle le chat avec `alias_demande: openweight-medium` et le modèle résolu correspond (Mistral-Small-3.2 au catalogue du 03/07) ; 4/4 ok. ⚠️ Si le pod exporte `ALBERT_MODEL_CHAT`, il **prime** sur le défaut — le vider pour tester le défaut (`unset ALBERT_MODEL_CHAT`).

## 3. Génération réelle (navigateur)

1. Relancer api + web (`make pod-up`) — le hot-reload ne recharge pas une variable d'env.
2. Dérouler une session courte (plan S2.6, étapes 0→3).
3. Attendu : réponses au gabarit du prompt 3 **sans** la dérive de format constatée avec large (`US 9 –`, verbosité ~1 150 tokens) ; le contrôle S1.10 ne signale pas de non-conformité systématique ; stories/feedback/export extraits normalement en aval.

## 4. Réversibilité (contre-épreuve)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
echo 'ALBERT_MODEL_CHAT=openweight-large' >> .env   # ou export dans la session
make probe
```

Attendu : `alias_demande: openweight-large` — retour en un geste, sans code. Retirer la ligne après la contre-épreuve.

## 5. Banc E6 (inchangé)

`make eval` compare toujours `openweight-large` vs `openweight-medium` par défaut : le banc reste l'instrument de la décision définitive au gold.

## 6. Clôture

Consigner le comportement de medium en session réelle dans `SESSIONS.md` (adhérence au gabarit, latence ressentie) ; si l'essai déçoit, revenir à large (étape 4) et rouvrir la story « adhérence format de large ».
