# Plan de test S2.7 — E5 : export CSV Jira + copier-coller formaté

**Environnement cible** : après le plan S2.6 (une session de workflow réelle ayant produit au moins une US à l'étape rédaction — avancer la session avec `/avancer` jusqu'à `redaction` puis demander une story au moteur).

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate && make lint && make test
```

Attendu : lint vert ; **146 tests verts** (dont 8 TU S2.7, `api/tests/test_export.py`).

## 2. Export CSV

```bash
cd ~/work/GRIAC/
curl -sS -D /tmp/entetes.txt http://localhost:8000/workflows/1/export/jira.csv -o /tmp/export.csv
grep -i "x-hypotheses-non-levees" /tmp/entetes.txt      # attendu : le compteur A8
head -2 /tmp/export.csv                                  # attendu : "Summary","Issue Type","Description"
python3 -c "import csv; print(sum(1 for _ in csv.reader(open('/tmp/export.csv'))) - 1, 'stories')"
```

Attendu : CSV bien formé (les champs multilignes sont quotés), une ligne par story dédupliquée.

## 3. Export markdown (copier-coller)

```bash
curl -sS http://localhost:8000/workflows/1/export/markdown | head -30
```

Attendu : si des hypothèses restent en attente — **avertissement `arbitrage A8` + liste en tête** ; chaque story précédée de son annotation de conformité S1.10 (`<!-- Contrôle S1.10 : conforme… -->`).

## 4. Garde-fous

```bash
# session sans story rédigée -> 409 explicite
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8000/workflows/<id-neuf>/export/jira.csv
```

## 5. Import Jira réel (quand un Jira de test sera accessible — hors dev, D10)

Importer `/tmp/export.csv` via l'assistant d'import CSV Jira : chaque ligne devient une Story, la Description porte le markdown intégral (marqueurs [HYPOTHÈSE À VALIDER] visibles).

## 6. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S2.7.
