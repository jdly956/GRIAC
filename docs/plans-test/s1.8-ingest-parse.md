# Plan de test S1.8 — Parsing docling → markdown structuré

**Environnement cible** : PostgreSQL migrée (0001→0003) + réseau sortant vers `huggingface.co` (docling télécharge ses modèles de layout/tableaux au **premier** parsing de PDF — bloqué sur les sessions Claude, OK attendu sur pod Onyxia). Mode A (`make dev`) ou mode B (service PostgreSQL Onyxia), comme S1.7.

⚠️ `make install` télécharge désormais docling/torch (~2 Go) au premier passage.

**Clôture** : consigner dans `SESSIONS.md`, cocher les CA de S1.8 (et ceux de S1.7 si son plan est joué dans la foulée).

## 0. Préparation

```bash
cd ~/work/GRIAC/
git fetch origin && git checkout <branche-de-la-PR-S1.8> && git pull
make install
source .venv/bin/activate
```

## 1. TNR — baseline complète

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **40 tests verts** (dont 9 TU S1.8, `ingestion/tests/test_parse.py`).

## 2. Base + migrations (0003 incluse)

Comme S1.7 étape 2 (mode A : `make dev` ; mode B : `alembic upgrade head` avec `DATABASE_URL` exportée). Attendu : `Running upgrade 0002 -> 0003`.

## 3. Préalable — inventaire du corpus (S1.7)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make ingest-scan CORPUS=evals/fixtures
```

Attendu : 6 fichiers scannés (insérés ou mis à jour).

## 4. Parsing du lot

```bash
make ingest-parse CORPUS=evals/fixtures ; echo "code retour = $?"
```

Attendu : `5 documents — 3 parsés, 1 inchangé (reprise sur hash), 1 ocr_requis, 0 échecs`, code retour **0**. Lecture : 3 docx + 2 pdf en périmètre (le txt est hors périmètre) ; la copie byte-à-byte et son original partagent le même sha256 → **un seul dérivé**, le second est « inchangé » ; le PDF scanné est dérouté en `ocr_requis` **sans** conversion (OCR = sprint 2).

## 5. CA1 — hiérarchie et tableaux dans les dérivés

```bash
ls derived/md/
cat derived/md/<un-sha>.md
```

Attendu : 3 fichiers `.md` ; le dérivé d'une spec docx montre `## Spécification…`, `### Critères d'acceptation` et un **tableau markdown** `| Critère | Valeur cible |` (déjà démontré en session sur la fixture docx ; le PDF natif est à confirmer ici — premier run = téléchargement des modèles docling).

## 6. Reprise sur hash — relance sans reconversion

```bash
make ingest-parse CORPUS=evals/fixtures
```

Attendu : `0 parsés, 4 inchangés (reprise sur hash), 1 ocr_requis, 0 échecs` — aucune reconversion (D9).

## 7. CA3 — un échec n'interrompt pas le lot

```bash
cp -r evals/fixtures /tmp/corpus-p
printf 'PK-corrompu' > "/tmp/corpus-p/projet-alpha/spec-authentification_v1.docx"
make ingest-scan CORPUS=/tmp/corpus-p        # ré-inventorie (sha modifié)
make ingest-parse CORPUS=/tmp/corpus-p ; echo "code retour = $?"
tail -5 derived/rapport-parsing.csv
```

Attendu : `… 1 échecs`, la ligne d'échec listée sur stderr **et** dans `derived/rapport-parsing.csv` (chemin + erreur), les autres documents traités normalement ; code retour 1 (échecs présents).

## 8. Statuts en base

```sql
select chemin, statut_parsing, chemin_derive is not null as derive, erreur_parsing
from documents order by chemin;
```

Attendu : `parse` (docx/pdf natif, avec dérivé), `ocr_requis` (scan, sans dérivé), `echec` (corrompu, avec erreur), `a_parser` → aucun, le txt reste `a_parser` (hors périmètre docx/pdf — à requalifier `hors_perimetre` si besoin en S1.9).

## 9. Clôture

Consigner les sorties (étapes 1, 4–8) dans `SESSIONS.md` ; cocher les CA de S1.8.
