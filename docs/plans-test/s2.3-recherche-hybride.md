# Plan de test S2.3 — RAG : recherche hybride BM25 + vecteurs

**Environnement cible** : base migrée 0001→0007 avec la chaîne d'ingestion jouée (plans S1.7 → S2.2 : fixtures scannées, parsées, qualifiées, chunkées, vectorisées) + api lancée avec la clé Albert (env du pod).

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **103 tests verts** (dont 8 TU S2.3, `api/tests/test_recherche.py`).

## 2. Migration 0007

```bash
cd ~/work/GRIAC/api/ && uv run alembic upgrade head
```

Attendu : `Running upgrade 0006 -> 0007` (index GIN plein-texte français).

## 3. Recherche nominale (api lancée comme au plan S1.11)

```bash
cd ~/work/GRIAC/
curl -sS -X POST http://localhost:8000/recherche -H "Content-Type: application/json" \
  -d '{"question": "temps de connexion au module d'\''authentification"}' | python3 -m json.tool
```

Attendu : des `resultats` avec **document + section** (citation traçable), `score_rrf` > 0, la spec v2 (référence) en tête ; `avertissement: null`.

## 4. Filtre « statut = référence » (défaut)

Par défaut seuls les documents `est_reference` sortent — la spec v1 (non référence) et la copie (doublon) ne doivent pas apparaître. Contre-épreuve :

```bash
curl -sS -X POST http://localhost:8000/recherche -H "Content-Type: application/json" \
  -d '{"question": "authentification", "seulement_references": false}' | python3 -m json.tool
```

Attendu : la v1 peut alors apparaître (périmètre élargi explicitement).

## 5. Filtre par projet (A6)

```bash
curl -sS -X POST http://localhost:8000/recherche -H "Content-Type: application/json" \
  -d '{"question": "authentification", "projet_id": 1}' | python3 -m json.tool
```

Attendu : uniquement des chunks des dossiers confirmés du projet 1 (S1.11) ; un projet sans dossier associé → aucun résultat + avertissement.

## 6. Aucune source récupérable — signalement anti-invention

```bash
curl -sS -X POST http://localhost:8000/recherche -H "Content-Type: application/json" \
  -d '{"question": "granularité des quotas de pêche en mer Baltique"}' | python3 -m json.tool
```

Attendu : `resultats: []` et `avertissement: "Aucune source récupérable…"`.

## 7. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S2.3 (`docs/sprint-2-backlog.md`).
