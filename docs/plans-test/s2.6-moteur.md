# Plan de test S2.6 — E3.2 : moteur conversationnel (première génération réelle)

**Environnement cible** : chaîne complète jouée (plans S1.7 → S2.5) + **clé Albert** (env du pod). C'est le premier test de génération de bout en bout : RAG réel + prompt 3 + gpt-oss-120b.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate && make lint && make test
```

Attendu : lint vert ; **138 tests verts** (dont 12 TU S2.6, `api/tests/test_moteur.py`).

## 2. Session + premier message (api lancée comme au plan S1.11)

```bash
cd ~/work/GRIAC/
curl -sS -X POST http://localhost:8000/workflows -H "Content-Type: application/json" \
  -d '{"feature": "Feature : suivi en ligne d'\''une demande usager. CA : consulter le statut ; être guidé quand une pièce est attendue.", "projet_id": 1}' | python3 -m json.tool

curl -sS -X POST http://localhost:8000/workflows/1/message -H "Content-Type: application/json" \
  -d '{"message": "Voici ma feature, aide-moi à la découper. Quelles informations te manquent ?"}' | python3 -m json.tool
```

Attendu observable :
- `reponse` en français, **restitution de la Feature + questions par lots de ≤ 3** (étape 0/1 du prompt 3) — si > 3 questions, un avertissement « règle 1 » apparaît ;
- `sources` : les extraits du corpus de fixtures mobilisés (panneau A3) ;
- `hypotheses_ajoutees` : toute proposition du modèle marquée [HYPOTHÈSE À VALIDER] entre au registre ;
- `avertissements` : vide, ou explicites (aucune source / budget / règle 1).

## 3. Question documentaire libre dans le fil (A2)

```bash
curl -sS -X POST http://localhost:8000/workflows/1/message -H "Content-Type: application/json" \
  -d '{"message": "Que dit la documentation sur le temps de connexion au module d'\''authentification ?"}' | python3 -m json.tool
```

Attendu : réponse **sourcée** (citations `[Source : …]` cohérentes avec `sources`), même moteur, pas d'écran dédié.

## 4. Divergence corpus↔PO (A9)

```bash
curl -sS -X POST http://localhost:8000/workflows/1/message -H "Content-Type: application/json" \
  -d '{"message": "Le temps de connexion visé est de 10 secondes."}' | python3 -m json.tool
```

Attendu : la spec des fixtures disant « moins d'une seconde », une ligne `[DIVERGENCE] …` avec source doit apparaître dans `divergences` (comportement dépendant du modèle — si absent, le noter : consigne de prompt à renforcer, pas un bug de plomberie).

## 5. Registre alimenté et A8 intact

```bash
curl -sS http://localhost:8000/workflows/1 | python3 -m json.tool
```

Attendu : les hypothèses du modèle sont `en_attente`, origine `modele` — aucune n'a été levée par les échanges.

## 6. Relevés à consigner (préparation E6)

Pour 2–3 messages : latence perçue, `avertissements` de budget éventuels, qualité de la restitution (grille 3 axes : gabarit, exactitude, complétude — à chaud, informel).

## 7. Clôture

Consigner dans `SESSIONS.md` (extraits de réponses réelles inclus), cocher les CA de S2.6.
