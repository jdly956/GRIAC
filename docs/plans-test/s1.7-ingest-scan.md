# Plan de test S1.7 — Ingestion : scan & inventaire du corpus

**Environnement cible** : une PostgreSQL avec les migrations appliquées —
- **mode A** (hôte avec daemon Docker) : `make dev` fournit tout (postgres+pgvector, migration 0002) — **exécuter ce plan en mode A lève aussi la réserve compose de S1.2** ;
- **mode B** (pod Onyxia sans daemon) : service **PostgreSQL** du catalogue Onyxia, puis migration à la main (étape 2).

**Clôture** : consigner les sorties dans `SESSIONS.md`, cocher les CA de S1.7.

## 0. Préparation

```bash
cd ~/work/GRIAC/
git fetch origin && git checkout <branche-de-la-PR-S1.7> && git pull
make install
source .venv/bin/activate
```

## 1. TNR — baseline complète

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint
make test
```

Attendu : lint vert ; **31 tests verts** (dont les 7 TU S1.7, `ingestion/tests/test_scan.py`).

## 2. Base de données + migrations

Mode A :

```bash
cd ~/work/GRIAC/
make dev          # postgres -> migrate (0001 pgvector, 0002 documents) -> api -> web
export DATABASE_URL="postgresql+psycopg://sia:sia_dev@localhost:5432/sia"
```

Mode B (service PostgreSQL Onyxia — relever hôte/mdp dans l'UI du service) :

```bash
cd ~/work/GRIAC/api/ && source ../.venv/bin/activate
export DATABASE_URL="postgresql+psycopg://<user>:<mdp>@<host-du-service>:5432/<db>"
uv run alembic upgrade head
```

Attendu : migrations 0001 puis 0002 appliquées (`alembic` affiche `Running upgrade 0001 -> 0002`). Ne jamais écrire l'URL dans un fichier versionné.

## 3. Scan initial des fixtures

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make ingest-scan CORPUS=evals/fixtures
```

Attendu : `6 fichiers scannés — 6 insérés, 0 mis à jour ; inventaire : derived/inventaire.csv`.

## 4. Idempotence — relance = zéro doublon

```bash
make ingest-scan CORPUS=evals/fixtures
# puis en base (mode A : make psql ; mode B : psql via l'URL) :
#   select count(*) from documents;
```

Attendu : `6 fichiers scannés — 0 insérés, 6 mis à jour` ; `count = 6` (aucun doublon créé).

## 5. Doublons de contenu visibles (prépare S1.9)

```sql
select sha256, count(*), array_agg(nom) from documents
group by sha256 having count(*) > 1;
```

Attendu : 1 ligne — `spec-authentification_v2_final_VF3.docx` et sa `- Copie.docx` partagent le même sha256 (chemins distincts, 2 lignes en base : la déduplication est le travail de S1.9, pas du scan).

## 6. Fichier modifié = mise à jour, pas de doublon

```bash
cp -r evals/fixtures /tmp/corpus-test
echo "ligne ajoutée pour test" >> "/tmp/corpus-test/projet-beta/notes-reunion-2026-06-15.txt"
make ingest-scan CORPUS=/tmp/corpus-test
```

Attendu : `0 insérés, 6 mis à jour` (mêmes chemins relatifs) ; en base, la ligne `notes-reunion…` a un nouveau `sha256`, `taille_octets` augmentée, `derniere_vue` > `premiere_vue`. C'est le mécanisme sur lequel s'appuiera la reprise sur hash (D9).

## 7. Inventaire CSV

```bash
head -3 derived/inventaire.csv
```

Attendu : en-tête `chemin,nom,extension,taille_octets,sha256,mtime` + lignes de données (7 lignes au total). `derived/` est hors git (`.gitignore`).

## 8. Refus explicite de s3://

```bash
make ingest-scan CORPUS=s3://bucket/corpus ; echo "code retour = $?"
```

Attendu : message `s3:// non pris en charge à ce stade : le snapshot MinIO n'existe pas encore…`, code retour non nul.

## 9. Clôture

Consigner les sorties (étapes 1, 3–8) dans `SESSIONS.md` ; cocher les CA de S1.7 ; si le plan a tourné en mode A, consigner aussi la levée de la réserve compose S1.2 (`make dev` + `make dev-validate` réels).
