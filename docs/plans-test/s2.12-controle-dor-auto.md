# Plan de test S2.12 — E3 : contrôle DoR/gabarit automatisé en sortie d'étape

**Environnement cible** : chaîne complète (base peuplée + clé Albert), api + web lancés (mode B pod, mêmes commandes que les plans S2.6/S2.8). Se joue naturellement **dans la foulée du plan S2.6** (première génération réelle) : c'est la même session poussée jusqu'aux étapes 3 et 4.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **203 tests verts** (dont 6 TU contrôle dans `test_moteur.py`).

## 2. Fumée sans réseau (fonction pure)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
uv run --package sia-api python -c "
from sia_api.moteur import controler_conformite
bancale = '---\n**US — Story bancale**\n\nDu texte sans blocs.\n---'
for r in controler_conformite('controle_dor', bancale):
    print('-', r[:110])
"
```

Attendu : deux lignes — « Contrôle gabarit (S1.10) — US « Story bancale » non conforme : … » et « Contrôle DoR automatisé (étape 4) : tableau DoR absent … ».

## 3. En session réelle (navigateur)

1. Mener une session jusqu'à l'**étape 3 — Rédaction** (plan S2.6) ; demander la rédaction des US.
2. Si le modèle produit une story incomplète : le panneau « Dernier échange » affiche l'avertissement « Contrôle gabarit (S1.10)… » avec le titre de l'US fautive. Si tout est conforme : aucun avertissement de contrôle (silence = conforme).
3. Valider « Oui » → **étape 4 — Contrôle DoR** ; demander le contrôle DoR.
4. Attendu : le modèle produit son tableau DoR ; l'avertissement « Contrôle DoR automatisé » n'apparaît **que** si le tableau est absent/incomplet (critère manquant, statut invalide, justification vide, « estimée et revue en backlog refinement » ≠ 🔵).
5. Contre-épreuve : demander au modèle « mets ✅ à la ligne estimation » → l'avertissement 🔵 doit apparaître (l'estimation relève de l'équipe, jamais de l'IA).

## 4. Non-blocage (règle 5)

Malgré les avertissements de contrôle, « Oui — étape suivante » fonctionne : le contrôle **signale, le PO arbitre**. Vérifier aussi qu'aucun contrôle n'apparaît aux étapes 0–2 (interview comprise).

## 5. Clôture

Consigner dans `SESSIONS.md`, cocher le CA « contrôle DoR automatisé » d'E3 (sprint-2, S2.12).
