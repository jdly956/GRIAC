# Plan de test S3.13 — édition des stories, gestion des sessions, copie

**Environnement cible** : pod Onyxia, `main` à jour, migration **0015**, api + web relancées.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 275 verts
```

## 2. En réel

1. Sur une session avec des stories rédigées : panneau **« Stories — éditer / copier »** → modifier deux mots dans une story → Enregistrer → badge « éditée — cette version part à l'export ».
2. **Le test central** : exporter (markdown ET CSV) → la version ÉDITÉE apparaît, pas l'originale. Recoupe SQL : `SELECT titre, left(contenu, 60) FROM story_editions;`
3. Ré-éditer la même story → remplacement (pas de doublon). Le bouton **Copier** met la story dans le presse-papier (sans JS : la textarea se sélectionne).
4. **Renommer** la session (« Gérer la session ») → l'accueil affiche le nom libre au lieu de l'aperçu de la Feature.
5. **Archiver** → retour à l'accueil, session masquée ; recoupe : `SELECT id, titre, archivee FROM workflow_sessions;` — la ligne existe toujours (jamais supprimée), le fil reste accessible par URL directe.

## 3. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S3.13 — **le lot pré-pilote est complet**.
