# Plan de test S3.11 — comptabilité tokens (global / session / ingestion)

**Environnement cible** : pod Onyxia, `main` à jour, migration **0011** (`cd api/ && uv run alembic upgrade head`), api + web relancées (`make pod-up`).

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 246 verts
```

## 2. En réel

1. Envoyer 2-3 messages dans une session → **l'entête de session affiche** « Consommation de la session : N appel(s) — X tokens entrée / Y sortie » et croît à chaque échange. Recoupe SQL : `SELECT * FROM conso_tokens WHERE session_id = <id>;` (source `chat`, modèle actif).
2. Relancer `make ingest-embed` après avoir invalidé un embedding (ou sur nouveaux chunks) → la sortie affiche les tokens et `SELECT * FROM conso_tokens WHERE source = 'embeddings';` porte une ligne par lot.
3. Écran **Télémétrie** : panneau « Consommation de tokens » — total, détail par source, **jauge du jour vs tpd 2,46 M** (surchargeable `ALBERT_TPD_QUOTA`).
4. Cas dégradé : l'endpoint conso indisponible ne casse ni l'écran session ni la télémétrie (indication simplement absente).

## 3. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S3.11 (les tokens « par import » se raffinent avec les runs S3.10).
