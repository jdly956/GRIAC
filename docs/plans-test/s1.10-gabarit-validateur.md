# Plan de test S1.10 — Gabarits internes : templates structurés & validateur

**Environnement cible** : n'importe quel environnement avec le repo installé (pod Onyxia, poste) — aucune base ni réseau requis.

## 1. TNR — baseline complète

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **75 tests verts** (dont 16 TU S1.10, `api/tests/test_gabarit.py` — les 3 silver servent de fixtures).

## 2. Validation réelle des candidates silver

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
uv run --package sia-api python -m sia_api.gabarit evals/silver/stories-silver-candidates.md ; echo "code retour = $?"
```

Attendu :

```
[CONFORME] Consulter l'état d'avancement de ma demande — 4 CA, 1 hypothèse(s) à valider, 0 avertissement(s)
[CONFORME] Déposer une pièce complémentaire demandée — 4 CA, 2 hypothèse(s) à valider, 0 avertissement(s)
[CONFORME] Être notifié par courriel d'un changement de statut — 3 CA, 3 hypothèse(s) à valider, 0 avertissement(s)
code retour = 0
```

Les hypothèses sont **relevées, jamais bloquantes** (marqueurs conservés sur les silver — A8).

## 3. Contre-épreuve — une story dégradée est refusée

```bash
cd ~/work/GRIAC/
sed 's/| # | Étant donné que… | Lorsque… | Alors… |/| # | Given | When | Then |/' \
  evals/silver/stories-silver-candidates.md > /tmp/silver-degradee.md
uv run --package sia-api python -m sia_api.gabarit /tmp/silver-degradee.md ; echo "code retour = $?"
```

Attendu : 3 × `[NON CONFORME]` avec la violation « colonnes du tableau des CA non conformes… », code retour **1**.

## 4. Clôture

Consigner dans `SESSIONS.md`. Rappel : le dernier CA de S1.10 (5–10 stories gold) reste une dépendance externe (extraction Jira / promotion des silver par les PO pilotes — statu quo du 02/07).
