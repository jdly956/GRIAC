# Plan de test S2.9 — E4.2 (écran projet) + E4.3 (écran « mes documents », A5)

**Environnement cible** : base réelle peuplée (plans S1.7 → S1.9 déroulés au minimum — les suggestions A6 et les statuts de parsing viennent de là) + api ET web lancés. Mode B pod :

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
uv run --package sia-api uvicorn sia_api.main:app --host 0.0.0.0 --port 8000 &
SIA_API_URL=http://localhost:8000 uv run --package sia-web uvicorn sia_web.main:app --host 0.0.0.0 --port 8081 &
# navigateur : https://<url-vscode>/proxy/8081/
```

## 1. TNR

```bash
cd ~/work/GRIAC/ && make lint && make test
```

Attendu : lint vert ; **166 tests verts** (dont 3 TU api `test_documents.py` et 16 TU web).

## 2. Endpoints api seuls (avant le navigateur)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
curl -s http://localhost:8000/documents | python3 -m json.tool | head -20
curl -s http://localhost:8000/documents/stats | python3 -m json.tool
```

Attendu : l'inventaire liste les fichiers du corpus scanné (chemin, `statut_parsing`, `est_reference`, `doublon`, `projet_suggere`) ; les stats donnent `couverture_parsing` = parsés/parsables (1.0 si rien à parser) — recouper avec le rapport de parsing S1.8.

## 3. Écran projets (E4.2) au navigateur

1. Menu « Projets » → la liste montre le projet du plan S1.11 (NFR et dossiers comptés).
2. Créer un projet : nom + contexte + **1 NFR remplie sur les 3 lignes** (ex. type `performance`, formulation `p95 < 1 s`, valeur `1 s`) → redirection vers le détail, la NFR apparaît dans le tableau.
3. Recréer un projet du même nom → l'écran reste sur le formulaire avec l'alerte « déjà existant » (pas de traceback).

## 4. Écran projet — association des dossiers (A6)

1. Sur le détail : les **suggestions S1.9 sont listées avec leur nombre de documents**, cochées seulement si déjà associées ; un dossier ajouté manuellement apparaît « (ajout manuel) ».
2. Cocher une suggestion + saisir un dossier libre → « Enregistrer l'association » → au rechargement les deux sont cochés.
3. Vérifier en base l'origine (suggestion vs po) :

```bash
kubectl exec -it <pod-postgres> -- psql -U <user> -d <db> \
  -c "SELECT dossier, origine FROM project_dossiers ORDER BY dossier;"
```

Attendu : la suggestion cochée porte `origine = 'suggestion'`, le dossier saisi `origine = 'po'` ; les nom/contexte/NFR du projet sont **inchangés** (le PUT les repasse tels quels).

## 5. Écran « mes documents » (E4.3, A5)

1. Menu « Mes documents » → panneau « État du corpus » (total, parsables, indexés, échecs, OCR requis, références) + inventaire avec statuts libellés (`indexé`, `échec`, `OCR requis`, `en attente`), badge `✔ référence`, doublons signalés, projet suggéré.
2. **Alerte couverture** : si `couverture_parsing < 0,8` (le corpus de fixtures contient un PDF « OCR requis » qui la fait baisser), le bandeau `fr-alert--warning` « Couverture documentaire faible » s'affiche avec le pourcentage ; sinon absent. Pour forcer le cas contraire, comparer avec un dossier 100 % docx.

## 6. Robustesse

Arrêter l'api puis recharger `/projets` et `/documents`. Attendu : alerte/page « API injoignable … » lisible — jamais de traceback.

## 7. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S2.9.
