# Plan de test S1.4 — Configuration & secrets (PR #4)

**Environnement cible** : pod Onyxia VSCode **sans GPU**, mode B (pas de daemon Docker — cas standard, cf. `docs/init-pod-onyxia.md`). L'étape 7, optionnelle, se joue sur un hôte avec daemon Docker (mode A) et lève au passage la réserve compose de S1.2.

**Préalable** : la clé Albert disponible sous la main (jamais versionnée, jamais dans l'historique shell). Durée estimée : ~10 min.

**Clôture** : consigner chaque résultat observé dans `SESSIONS.md` (règle « validation stack-live »).

## 0. Préparation

```bash
cd ~/work/GRIAC/
git fetch origin claude/backlog-continuation-6ftff4
git checkout claude/backlog-continuation-6ftff4 && git pull origin claude/backlog-continuation-6ftff4
make install
source .venv/bin/activate
```

Attendu : sync uv OK, branche de la PR #4 en place.

⚠️ **Si un `.env` existe déjà à la racine, le neutraliser pour les étapes 1 à 3** (`mv .env .env.bak`) : il serait lu automatiquement et fausserait les tests d'absence de configuration.

## 1. TNR — baseline complète (TU inclus)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint
make test
```

Attendu : lint vert ; **13 tests verts**, dont les 8 TU de S1.4 (`api/tests/test_config.py`).

## 2. CA3 — refus de démarrer sans clé (échec propre, message explicite)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
env -u ALBERT_BASE_URL -u ALBERT_API_KEY uv run --package sia-api uvicorn sia_api.main:app --port 8000
echo "code retour = $?"
```

Attendu : `RuntimeError: Configuration Albert invalide — variables d'environnement manquantes ou vides : ALBERT_API_KEY, ALBERT_BASE_URL…`, puis `Application startup failed. Exiting.` ; code retour **3** (non nul). Les **noms** des variables apparaissent, jamais une valeur.

## 3. Variante compose — variables présentes mais vides

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
ALBERT_BASE_URL="" ALBERT_API_KEY="" uv run --package sia-api uvicorn sia_api.main:app --port 8000
```

Attendu : même refus qu'à l'étape 2 (une variable vide vaut absence — cas `${VAR:-}` du compose).

## 4. CA1/CA2 — configuration nominale par `.env`

```bash
cd ~/work/GRIAC/
cp .env.example .env
# Renseigner ALBERT_API_KEY=<clé> dans .env avec l'éditeur — pas en ligne de commande.
git status --short          # attendu : .env n'apparaît PAS (ignoré par git)
source .venv/bin/activate
uv run --package sia-api uvicorn sia_api.main:app --host 0.0.0.0 --port 8000 > /tmp/api-s14.log 2>&1 &
sleep 2 && curl -fsS http://localhost:8000/health
```

Attendu : `{"status":"ok"}` ; `.env` invisible pour git.

## 5. CA3 — la clé n'apparaît ni dans les logs ni dans les repr

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
CLE=$(grep '^ALBERT_API_KEY=' .env | cut -d= -f2-)
grep -c -- "$CLE" /tmp/api-s14.log || echo "OK : 0 occurrence de la clé dans le log"
uv run --package sia-api python -c "from sia_api.config import charger_settings; print(charger_settings())"
```

Attendu : `0` occurrence dans le log de démarrage ; le `print` affiche `albert_api_key=SecretStr('**********')` (jamais la valeur) et les alias `openweight-large` / `openweight-embeddings` / `openweight-rerank`.

## 6. Nettoyage (mode B)

```bash
kill %1
rm -f /tmp/api-s14.log
```

## 7. Optionnel — hôte avec daemon Docker (mode A) : compose complet

```bash
cd <racine du repo>
mv .env .env.bak 2>/dev/null ; make dev      # attendu : échec — le service api ne devient pas sain
docker compose -f infra/compose.yaml --project-directory . logs api | tail -5
# attendu : « Configuration Albert invalide … » dans les logs du service api
mv .env.bak .env ; make dev && make dev-validate
```

Attendu : sans `.env`, api en échec avec le message explicite dans `docker compose logs api` ; avec `.env`, stack saine et `make dev-validate` vert (`/health` api + web = 200, extension pgvector présente) — **lève aussi la réserve compose S1.2**, à consigner dans `SESSIONS.md`.

## 8. Clôture

Reporter dans `SESSIONS.md` : sorties observées des étapes 1–5 (et 7 le cas échéant), écarts éventuels, verdict ✅/❌ par critère d'acceptation.
