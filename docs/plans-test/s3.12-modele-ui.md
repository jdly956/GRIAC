# Plan de test S3.12 — changement de modèle depuis l'UI (réglage global instance)

**Environnement cible** : pod Onyxia, `main` à jour, migration **0012**, api + web relancées.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 253 verts
```

## 2. En réel

1. Écran **Paramètres** : modèle actif affiché « (défaut de l'instance) ».
2. Sélectionner `openweight-large` → Appliquer → badge « surcharge UI » ; **sans relancer l'api**, envoyer un message dans une session → l'entête de session affiche « Modèle : openweight-large » et `SELECT modele FROM conso_tokens ORDER BY id DESC LIMIT 1;` confirme le modèle utilisé.
3. Champ libre : saisir un alias fantaisiste → Appliquer → le prochain message échoue proprement (erreur Albert lisible, pas de traceback) — revenir au défaut.
4. **« Revenir au défaut de l'instance »** → badge disparu, le défaut (env `ALBERT_MODEL_CHAT` sinon code) reprend la main.

## 3. Précédence (rappel)

Surcharge UI (base) > `ALBERT_MODEL_CHAT` (env, lu au démarrage) > défaut du code (`openweight-medium`, S2.14). Le banc `make eval` reste l'instrument de comparaison.

## 4. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S3.12.
