# Plan de test S3.7 — écran session réorganisé (navigation)

**Environnement cible** : pod Onyxia, `main` à jour après merge de la PR S3.7, web relancée (`make pod-up`). Aucune migration.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 237 verts
```

## 2. En session réelle (navigateur)

1. Ouvrir une session longue existante (ex. session 11). **Attendus** :
   - le **registre des hypothèses est replié** (une ligne-résumé avec le compteur) — la page commence par la zone d'action, plus par 18 hypothèses ;
   - le **fil est replié** : seuls les 4 derniers messages sont visibles, les précédents derrière « Voir les N échanges précédents » (cliquable) ;
   - le panneau « Noter les stories » est replié.
2. Envoyer un message. **Attendu (le test central)** : au retour, la **réponse rendue et sa traçabilité (sources, avertissements, levées) sont EN HAUT de page** — rien à scroller ; le formulaire de message est juste en dessous.
3. Provoquer une levée proposée (message qui tranche une hypothèse en attente, cf. plan s2.13). **Attendu** : le registre **se déplie tout seul** avec « … dont 1 levée(s) proposée(s) à décider » — c'est le seul cas où il réclame l'attention.
4. Étapes de production : « Story suivante » et « Valider l'étape » sont regroupés dans la zone d'action sous le formulaire.

## 3. Non-régression adjacente

- Boutons Confirmer/Rejeter fonctionnels dans le registre déplié ; badges S2.13 intacts.
- Rendu markdown (S3.6) appliqué au fil ET à la « Dernière réponse » en haut.
- Notation et exports inchangés.

## 4. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S3.7 (sprint-3).
