# Plan de test S3.9 — traçabilité persistée + extrait exact (A3 complet)

**Environnement cible** : pod Onyxia, `main` à jour, migration **0014**, api + web relancées.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 268 verts
```

## 2. En réel

1. Envoyer un message qui mobilise le corpus → la « Dernière réponse » montre les sources avec **« extrait exact »** dépliable : le texte du chunk cité, mot pour mot (la promesse A3).
2. **Recharger la page** (le test central — fin de la « v1 assumée » S2.8) : dans le fil, le message assistant garde « Sources mobilisées (N) » avec les extraits, ses avertissements et ses divergences. Recoupe SQL : `SELECT type, nom, left(extrait, 40), left(contenu, 40) FROM message_traces ORDER BY id DESC LIMIT 10;`
3. Provoquer une divergence (affirmer un délai contraire à la spec) → l'alerte A9 persiste au rechargement, rattachée au bon message.
4. Les messages antérieurs à la migration n'ont pas de traces (normal — seules les nouvelles réponses en portent).

## 3. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S3.9.
