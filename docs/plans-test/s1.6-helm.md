# Plan de test S1.6 — Charts Helm minimaux Onyxia

**Environnements cibles** : (1) la CI de la PR — `helm lint` + `helm template` (CA2, rendu valide) ; (2) un pod Onyxia avec `helm`/`kubectl` pour le déploiement réel dans le namespace personnel (lève au passage la réserve compose S1.2 si les images sont buildées en mode A au préalable).

**Clôture** : consigner dans `SESSIONS.md`, cocher les CA de S1.6.

## 1. CA2 (rendu) — sur la PR de la story

Onglet Checks : le job **`helm-chart`** (helm lint + helm template en valeurs par défaut) est vert. Résultat observable : conclusion `success`.

## 2. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test        # attendu : 59 tests verts (le chart n'ajoute pas de python)
```

## 3. Rendu local sur le pod (helm est présent sur les images Onyxia)

```bash
cd ~/work/GRIAC/
helm lint infra/helm/sia-po
helm template sia-po infra/helm/sia-po | head -40
helm template sia-po infra/helm/sia-po | grep -ci "gpu" || echo "OK : aucune demande de GPU"
```

Attendu : lint sans erreur ; manifestes rendus (Secret, PVC, Deployments postgres/api/web, Job migrate avec hooks, Services, Ingress) ; **0 occurrence de gpu** (CA3).

## 4. CA1 — déploiement réel sur le lab

Suivre pas-à-pas `docs/deploy-onyxia.md` (prérequis : images poussées, secret `sia-albert` créé). Attendu :

```bash
kubectl get pods           # postgres/api/web Running, migrate Completed
kubectl logs job/sia-po-migrate | tail -3     # Running upgrade ... -> 0005
curl -fsS https://sia-api.<utilisateur>.lab.sspcloud.fr/health     # {"status":"ok"}
curl -fsS https://sia-web.<utilisateur>.lab.sspcloud.fr/health     # {"status":"ok"}
curl -fsS https://sia-api.<utilisateur>.lab.sspcloud.fr/projects   # []
```

## 5. Nettoyage et clôture

```bash
helm uninstall sia-po      # PVC conservé ; kubectl delete pvc sia-po-pgdata pour purger
```

Consigner dans `SESSIONS.md` : job CI vert (étape 1), sorties des étapes 3–4, registre d'images retenu. Cocher les CA.
