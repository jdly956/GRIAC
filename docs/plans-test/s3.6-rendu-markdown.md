# Plan de test S3.6 — rendu markdown des messages du moteur

**Environnement cible** : pod Onyxia, `main` à jour après merge de la PR S3.6 (`git checkout main && git pull origin main`), `uv sync --all-packages` (nouvelle dépendance `markdown-it-py`), api + web relancées (`make pod-up`). Aucune migration.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
uv sync --all-packages && make lint && make test        # attendu : 234 verts
```

## 2. Fumée sans réseau (filtre)

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
uv run --package sia-web python -c "
from sia_web.main import rendre_markdown
html = rendre_markdown('**gras**\n\n| A | B |\n|---|---|\n| 1 | 2 |\n\n<script>x</script>')
print('<table>' in html, '<strong>' in html, '&lt;script&gt;' in html, '<script>' not in html)
"
```

Attendu : `True True True True` — tableau et gras rendus, HTML source échappé.

## 3. En session réelle (navigateur) — le test visuel

1. Ouvrir une session existante ayant des stories rédigées (ex. session 11) — **aucune régénération nécessaire**, le rendu s'applique au fil persistant.
2. **Attendus** : les `**US — Titre**` s'affichent en gras, les tableaux Gherkin des CA et les tableaux DoR sont de **vrais tableaux** (bordures, entêtes) — plus aucun `|` brut côté assistant ; les listes à puces/numéros sont mises en forme ; un tableau très large défile horizontalement dans son bloc sans casser la page.
3. Les messages **PO** restent en texte brut (un `**test**` tapé par le PO s'affiche tel quel).
4. Envoyer un message → la nouvelle réponse est rendue comme le reste du fil.

## 4. Non-régression adjacente

- Badges « Levée proposée », panneau sources, avertissements : inchangés (hors du fil).
- Exports E5 : toujours le markdown BRUT (c'est voulu — Jira/copier-coller le consomment tel quel).

## 5. Clôture

Consigner dans `SESSIONS.md` (capture avant/après bienvenue), cocher les CA de S3.6 (sprint-3).
