# Plan de test S1.11 — Entité Projet : contexte & NFR (CRUD API)

**Environnement cible** : PostgreSQL migrée (0001→0005) + api lancée. Mode A (`make dev` — l'api du compose reçoit DATABASE_URL) ou mode B (service PostgreSQL Onyxia + uvicorn, la clé Albert est dans l'env du pod). Enchaînable après les plans S1.7 → S1.9 (les suggestions s'appuient sur les fixtures qualifiées).

**Clôture** : consigner dans `SESSIONS.md`, cocher les CA de S1.11.

## 0. Préparation

```bash
cd ~/work/GRIAC/
git fetch origin && git checkout <branche-de-la-PR-S1.11> && git pull
make install && source .venv/bin/activate
export DATABASE_URL="postgresql+psycopg://<...>"
```

## 1. TNR — baseline complète

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **59 tests verts** (dont 10 TU S1.11, `api/tests/test_projets.py`).

## 2. Migration 0005

```bash
cd ~/work/GRIAC/api/ && uv run alembic upgrade head
```

Attendu : `Running upgrade 0004 -> 0005` (tables projects, project_nfrs, project_dossiers).

## 3. Prérequis suggestions — fixtures scannées et qualifiées

```bash
cd ~/work/GRIAC/
make ingest-scan CORPUS=evals/fixtures && make ingest-qualify
```

## 4. Lancer l'api (mode B)

```bash
uv run --package sia-api uvicorn sia_api.main:app --host 0.0.0.0 --port 8000 &
sleep 2 && curl -fsS http://localhost:8000/health
```

## 5. Suggestions A6 (issues de S1.9)

```bash
curl -sS http://localhost:8000/dossiers/suggestions | python3 -m json.tool
```

Attendu : `projet-alpha` (3 documents) et `projet-beta` (3 documents), `deja_associe: false`.

## 6. Création d'un projet complet

```bash
curl -sS -X POST http://localhost:8000/projects -H "Content-Type: application/json" -d '{
  "nom": "SIA PO",
  "contexte": "Assistant de rédaction de user stories pour PO.",
  "nfrs": [
    {"type": "performance", "formulation": "p95 de génération < 60 s", "valeur_cible": "60 s"},
    {"type": "accessibilite_rgaa", "formulation": "conformité RGAA 4 niveau AA"}
  ],
  "dossiers": [{"dossier": "projet-alpha", "origine": "suggestion"}]
}' | python3 -m json.tool
```

Attendu : **201**, `id: 1`, nfrs et dossiers restitués.

## 7. Lecture

```bash
curl -sS http://localhost:8000/projects | python3 -m json.tool
curl -sS http://localhost:8000/projects/1 | python3 -m json.tool
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8000/projects/99   # attendu : 404
```

## 8. Mise à jour (remplacement nfrs/dossiers)

```bash
curl -sS -X PUT http://localhost:8000/projects/1 -H "Content-Type: application/json" -d '{
  "nom": "SIA PO",
  "contexte": "Contexte enrichi après cadrage.",
  "nfrs": [{"type": "rgpd", "formulation": "aucune donnée personnelle dans les prompts"}],
  "dossiers": [{"dossier": "projet-alpha", "origine": "suggestion"}, {"dossier": "projet-beta", "origine": "po"}]
}' | python3 -m json.tool
curl -sS http://localhost:8000/projects/1 | python3 -m json.tool   # attendu : contexte modifié, 1 NFR rgpd, 2 dossiers
```

## 9. Garde-fous

```bash
# nom dupliqué -> 409
curl -sS -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8000/projects \
  -H "Content-Type: application/json" -d '{"nom": "SIA PO"}'
# type NFR hors liste -> 422
curl -sS -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8000/projects \
  -H "Content-Type: application/json" -d '{"nom": "X", "nfrs": [{"type": "esthetique", "formulation": "joli"}]}'
```

## 10. Boucle A6 fermée

```bash
curl -sS http://localhost:8000/dossiers/suggestions | python3 -m json.tool
```

Attendu : `projet-alpha` et `projet-beta` passent à `deja_associe: true` (suggestion confirmée/corrigée par le PO).

## 11. En base

```sql
select p.nom, n.type, n.formulation, n.valeur_cible from projects p
left join project_nfrs n on n.project_id = p.id;
select p.nom, d.dossier, d.origine from projects p join project_dossiers d on d.project_id = p.id;
```

## 12. Clôture

`kill %1` ; consigner les sorties (étapes 1, 5–10) dans `SESSIONS.md` ; cocher les CA de S1.11.
