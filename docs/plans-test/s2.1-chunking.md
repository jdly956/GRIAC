# Plan de test S2.1 — Chunking par sections (E1, nœud D)

**Environnement cible** : base PostgreSQL migrée (0001→0006) avec les fixtures scannées, parsées (plans S1.7/S1.8 déjà joués sur la même base). S'insère dans la chaîne de validation Onyxia après S1.8.

## 1. TNR

```bash
cd ~/work/GRIAC/ && source .venv/bin/activate
make lint && make test
```

Attendu : lint vert ; **88 tests verts** (dont 12 TU S2.1, `ingestion/tests/test_chunk.py`).

## 2. Migration 0006

```bash
cd ~/work/GRIAC/api/ && uv run alembic upgrade head
```

Attendu : `Running upgrade 0005 -> 0006` (table chunks, colonne embedding vector(1024)).

## 3. Chunking des fixtures parsées

```bash
cd ~/work/GRIAC/ && make ingest-chunk ; echo "code retour = $?"
```

Attendu : `3 documents chunkés (N chunks), 0 inchangés (reprise sur hash), 0 échecs`, exit 0 — les fixtures sont petites : attendre ~1 chunk par document (3 dérivés distincts : v1, v2, pdf natif — la copie partage le dérivé de la v2).

## 4. Contenu en base

```sql
select document_chemin, ordinal, section, nb_tokens, embedding is null as sans_embedding
from chunks order by document_chemin, ordinal;
```

Attendu : chaque chunk porte son **fil de titres** (ex. `Spécification authentification — v2 finale > Critères d'acceptation`), `nb_tokens` > 0, `embedding` NULL (rempli par S2.2). Les tableaux markdown sont entiers dans leur chunk (`contenu` contient l'en-tête ET toutes les lignes).

## 5. Reprise sur hash

```bash
make ingest-chunk
```

Attendu : `0 documents chunkés, 3 inchangés (reprise sur hash), 0 échecs`.

## 6. Clôture

Consigner dans `SESSIONS.md`, cocher les CA de S2.1 (`docs/sprint-2-backlog.md`).
